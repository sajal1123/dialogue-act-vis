import React from 'react';

function WordCloudFooter(props) {
    return (
        <div>
            Hover over the bubbles to see more details
        </div>
    );
}

export default WordCloudFooter;